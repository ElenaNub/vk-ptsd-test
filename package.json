{
  "name": "vk-ptsd-test",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://ElenaNub.github.io/vk-ptsd-test/",
  "dependencies": {
    "@vkontakte/icons": "^2.170.0",
    "@vkontakte/vk-bridge": "^2.8.1",
    "@vkontakte/vkui": "^4.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "^3.0.1"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts --openssl-legacy-provider build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build -f"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
